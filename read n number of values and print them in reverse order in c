#include<stdio.h>
void main()
{
int n,i;
printf("no. of elements in a array:");
scanf("%d",&n);
int arr[n];
printf("Enter %d elements:\n", n);
for(i=0;i<n;i++)
{
scanf("%d",&arr[i]);
}
printf("elements in reverse order:\n");
for(i=n-1;i>=0;i--)
{
printf("%d\t",arr[i]);
}
}
